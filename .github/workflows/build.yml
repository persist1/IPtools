name: Build Multi-Platform

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  workflow_dispatch:
    inputs:
      platform:
        description: '选择构建平台'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - windows-x64
          - windows-x86
          - linux-x86_64
          - linux-arm64
          - macos-x86_64
          - macos-arm64

env:
  QT_VERSION: '5.15.2'

jobs:
  build-windows:
    name: Build Windows (${{ matrix.arch }})
    runs-on: windows-latest
    # 只在满足以下条件时运行：
    # 1. 自动触发（push/PR）时
    # 2. 手动触发且选择了 all 或对应的平台
    if: |
      github.event_name != 'workflow_dispatch' || 
      github.event.inputs.platform == 'all' || 
      startsWith(github.event.inputs.platform, 'windows')
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: win64_mingw81
            mingw_arch: x86_64
            mingw_tool_dir: mingw81_64
            mingw_tool_variant: mingw81_64
            platform_name: windows-x64
          - arch: win32_mingw81
            mingw_arch: i686
            mingw_tool_dir: mingw81_32
            mingw_tool_variant: mingw81_32
            platform_name: windows-x86
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
    
    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: ${{ env.QT_VERSION }}
        host: 'windows'
        target: 'desktop'
        arch: ${{ matrix.arch }}
        modules: 'qtnetworkauth'
        tools: 'tools_mingw,${{ matrix.mingw_tool_variant }}'
        cache: true
    
    - name: Prepare Qt and MinGW Environment
      shell: pwsh
      run: |
        if (-not $env:Qt5_Dir) {
          throw "Qt5_Dir 环境变量未设置，install-qt-action 可能失败了"
        }

        Write-Host "Qt5_Dir: $env:Qt5_Dir"

        $pathsToAdd = @()

        # Qt bin 目录
        $qtBin = Join-Path $env:Qt5_Dir 'bin'
        if (Test-Path $qtBin) {
          $pathsToAdd += $qtBin
        }

        # 自动查找 Qt Tools 下的 mingw
        $qtRoot = (Get-Item $env:Qt5_Dir).Parent.Parent.FullName
        $toolsDir = Join-Path $qtRoot 'Tools'
        if (Test-Path $toolsDir) {
          $mingwDirs = Get-ChildItem $toolsDir -Directory | Where-Object { $_.Name -like 'mingw*' }
          foreach ($dir in $mingwDirs) {
            $binDir = Join-Path $dir.FullName 'bin'
            if (Test-Path $binDir) {
              $pathsToAdd += $binDir
            }
          }
        }

        # 去重并写入 GITHUB_PATH
        $pathsToAdd = $pathsToAdd | Sort-Object -Unique
        foreach ($path in $pathsToAdd) {
          Write-Host "Adding to PATH: $path"
          $path | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
        }

        Write-Host ""
        Write-Host "=== 工具版本检查 ==="
        & qmake -v
        & g++ --version | Select-Object -First 1
        & mingw32-make --version | Select-Object -First 1
        Write-Host "=== 工具准备完成 ==="
    
    - name: Build Project
      shell: cmd
      run: |
        echo Building IPtools...
        qmake IPtools.pro CONFIG+=release
        mingw32-make -j4
    
    - name: Deploy Qt Dependencies
      shell: cmd
      run: |
        cd release
        windeployqt IPtools.exe --release --no-translations
    
    - name: Create Archive
      shell: pwsh
      run: |
        $archName = "IPtools-Windows-${{ matrix.mingw_arch }}"
        New-Item -ItemType Directory -Force -Path dist
        Copy-Item -Path release/IPtools.exe -Destination dist/
        Copy-Item -Path README.md -Destination dist/ -ErrorAction SilentlyContinue
        Copy-Item -Path USAGE_GUIDE.md -Destination dist/ -ErrorAction SilentlyContinue
        Copy-Item -Path LICENSE -Destination dist/ -ErrorAction SilentlyContinue
        Copy-Item -Path release/*.dll -Destination dist/ -ErrorAction SilentlyContinue
        Compress-Archive -Path dist/* -DestinationPath "$archName.zip"
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: IPtools-Windows-${{ matrix.mingw_arch }}
        path: IPtools-Windows-${{ matrix.mingw_arch }}.zip
        retention-days: 30

  build-linux:
    name: Build Linux (${{ matrix.arch }})
    runs-on: ubuntu-20.04
    if: |
      github.event_name != 'workflow_dispatch' || 
      github.event.inputs.platform == 'all' || 
      startsWith(github.event.inputs.platform, 'linux')
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            qt_arch: gcc_64
            platform_name: linux-x86_64
          - arch: aarch64
            qt_arch: gcc_64
            platform_name: linux-arm64
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
    
    - name: Install Dependencies (x86_64)
      if: matrix.arch == 'x86_64'
      run: |
        sudo apt-get update
        sudo apt-get install -y qt5-default qtbase5-dev qttools5-dev-tools \
          build-essential lsof libgl1-mesa-dev
    
    - name: Install Cross-Compilation Tools (ARM64)
      if: matrix.arch == 'aarch64'
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
          qt5-default qtbase5-dev qttools5-dev-tools build-essential
    
    - name: Build Project (x86_64)
      if: matrix.arch == 'x86_64'
      run: |
        qmake IPtools.pro CONFIG+=release
        make -j$(nproc)
    
    - name: Build Project (ARM64)
      if: matrix.arch == 'aarch64'
      run: |
        # Note: Full ARM64 cross-compilation requires ARM64 Qt libraries
        # This is a placeholder - actual cross-compilation needs proper Qt setup
        qmake IPtools.pro CONFIG+=release
        make -j$(nproc) CXX=aarch64-linux-gnu-g++ CC=aarch64-linux-gnu-gcc || echo "ARM64 cross-compilation requires ARM64 Qt libraries"
    
    - name: Create AppImage (x86_64)
      if: matrix.arch == 'x86_64'
      run: |
        sudo apt-get install -y libfuse2
        wget https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
        wget https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
        chmod +x linuxdeploy*.AppImage
        mkdir -p AppDir/usr/bin
        cp IPtools AppDir/usr/bin/
        cp README.md AppDir/usr/ || true
        ./linuxdeploy-x86_64.AppImage --appdir AppDir --plugin qt --output appimage || echo "AppImage creation failed, continuing..."
    
    - name: Create Archive
      run: |
        mkdir -p dist
        if [ -f "IPtools" ]; then
          cp IPtools dist/
        fi
        cp README.md dist/ || true
        cp USAGE_GUIDE.md dist/ || true
        cp LICENSE dist/ || true
        tar -czf IPtools-Linux-${{ matrix.arch }}.tar.gz -C dist .
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: IPtools-Linux-${{ matrix.arch }}
        path: |
          IPtools-Linux-${{ matrix.arch }}.tar.gz
          *.AppImage
        retention-days: 30

  build-macos:
    name: Build macOS (${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    if: |
      github.event_name != 'workflow_dispatch' || 
      github.event.inputs.platform == 'all' || 
      startsWith(github.event.inputs.platform, 'macos')
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-12
            arch: x86_64
            platform_name: macos-x86_64
          - os: macos-14
            arch: arm64
            platform_name: macos-arm64
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
    
    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: ${{ env.QT_VERSION }}
        host: 'mac'
        target: 'desktop'
        modules: 'qtnetworkauth'
        cache: true
    
    - name: Build Project
      run: |
        qmake IPtools.pro CONFIG+=release
        make -j$(sysctl -n hw.ncpu)
    
    - name: Create App Bundle
      run: |
        mkdir -p dist
        cp -r IPtools.app dist/
        macdeployqt dist/IPtools.app -dmg
        cp README.md dist/ || true
        cp USAGE_GUIDE.md dist/ || true
        cp LICENSE dist/ || true
    
    - name: Create Archive
      run: |
        cd dist
        if [ -f "IPtools.dmg" ]; then
          mv IPtools.dmg ../IPtools-macOS-${{ matrix.arch }}.dmg
        else
          tar -czf ../IPtools-macOS-${{ matrix.arch }}.tar.gz .
        fi
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: IPtools-macOS-${{ matrix.arch }}
        path: |
          IPtools-macOS-${{ matrix.arch }}.*
        retention-days: 30

  build-summary:
    name: Build Summary
    needs: [build-windows, build-linux, build-macos]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: Summary
      run: |
        echo "## 🎯 构建结果" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # 显示触发方式
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "**触发方式**: 手动触发" >> $GITHUB_STEP_SUMMARY
          echo "**选择平台**: \`${{ github.event.inputs.platform }}\`" >> $GITHUB_STEP_SUMMARY
        else
          echo "**触发方式**: 自动触发 (${{ github.event_name }})" >> $GITHUB_STEP_SUMMARY
        fi
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "| 平台 | 架构 | 状态 |" >> $GITHUB_STEP_SUMMARY
        echo "|------|------|------|" >> $GITHUB_STEP_SUMMARY
        echo "| 🪟 Windows | x64 | ${{ needs.build-windows.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🪟 Windows | x86 | ${{ needs.build-windows.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🐧 Linux | x86_64 | ${{ needs.build-linux.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🐧 Linux | ARM64 | ${{ needs.build-linux.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🍎 macOS | x86_64 | ${{ needs.build-macos.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🍎 macOS | ARM64 | ${{ needs.build-macos.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "📦 构建产物可在 Actions 标签页的 Artifacts 中下载。" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "⏱️ 产物保留时间：30 天" >> $GITHUB_STEP_SUMMARY

